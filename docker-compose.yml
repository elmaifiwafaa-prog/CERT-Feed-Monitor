services:
  cert-watcher:
    build: .
    env_file: .env
    ports:
      - "${GMAIL_OAUTH_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
    restart: unless-stopped
